<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="AssetSql">
	<resultMap id="Asset" class="com.respace.domain.Asset">
		<result column="ID" property="id" />
		<result column="NAME" property="name" />
		<result column="URL" property="url" />
		<result column="TYPE" property="type" />
		<result column="ID_REFERENCE" property="id_reference" />
		<result column="SEQ_NO" property="seq_no" />
		<result column="REGISTERED_DATE" property="registered_date" />
		<result column="FILENAME" property="filename" />
		<result column="FILETYPE" property="filetype" />
		<result column="FILESIZE" property="filesize" />
		<result column="MODIFIED_DATE" property="modified_date" />
		<result column="FILEPATH" property="filepath" />
		<result column="CODE_STATUS" property="code_status" />
		<result column="ETC" property="etc" />
 	</resultMap>
 	
 	<select id="readAssetList" resultMap="Asset">
 		SELECT id, name, url, type, id_reference, seq_no, registered_date, filename, filetype, filesize, modified_date, filepath, code_status, etc
    	FROM rs_asset
    	WHERE
    	1=1
    	<isGreaterThan property="id_reference" compareValue="0">
    	and id_reference = #id_reference#
    	</isGreaterThan>
    	
    	<isNotEmpty property="code_status">
    	and code_status = #code_status#
    	</isNotEmpty>

    	<isGreaterThan property="id_reference" compareValue="0">
    	ORDER BY seq_no
    	</isGreaterThan>
    	<isLessEqual property="id_reference" compareValue="0">
    	ORDER BY registered_date desc
    	</isLessEqual>
 	</select>
 	
	<select id="readAsset" resultMap="Asset">
       SELECT id, name, url, type, id_reference, seq_no, registered_date, filename, filetype, filesize, modified_date, filepath, code_status, etc
       FROM rs_asset
       WHERE ID = #id# 
	</select>

	<insert id="createAsset" parameterClass="com.respace.domain.Asset">
		INSERT INTO 
		rs_asset (`name`, `url`, `type`, `id_reference`, `seq_no`, `registered_date`, `filename`, `filetype`, `filesize`, `modified_date`, `filepath`, `code_status`, `etc`) 
		VALUES (#name#, #url#, #type#, #id_reference#, #seq_no#, DATE_FORMAT(now(), "%Y%m%d%H%i%s"), #filename#, #filetype#, #filesize#, DATE_FORMAT(now(), "%Y%m%d%H%i%s"), #filepath#, #code_status#, #etc#)
	</insert>
	
	<update id="updateAsset" parameterClass="com.respace.domain.Asset">
		 UPDATE rs_asset SET
		 ID = #id#
		 <isNotEmpty property="url">
		 ,url = #url#
		 ,registered_date = DATE_FORMAT(now(), "%Y%m%d%H%i%s")
		 </isNotEmpty>
		 WHERE ID = #id# 
	</update>
	
	<delete id="deleteAsset" parameterClass="com.respace.domain.Asset">
		 DELETE FROM rs_asset
		 WHERE ID = #id#
	</delete>
</sqlMap>


