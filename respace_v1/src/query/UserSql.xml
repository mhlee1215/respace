<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="UserSql">

	<resultMap id="User" class="com.respace.domain.RS_User">
		<result column="ID" property="id" />
		<result column="NAME" property="name" />
		<result column="EMAIL" property="email" />
		<result column="TYPE" property="type" />
		<!-- <result column="PASSWORD" property="password" /> -->
		<result column="ISVERIFIED" property="isverified" />
		<result column="ISDELETED" property="isdeleted" />
		<result column="REGISTEREDTIME" property="registeredtime" />
		<result column="DELETEDTIME" property="deletedtime" />
		<result column="VERIFIEDTIME" property="verifiedtime" />
 	</resultMap>
 	
 	
 	
 	<select id="readUserList" resultMap="User">
 		SELECT ID, NAME, EMAIL, TYPE, ISVERIFIED, ISDELETED, REGISTEREDTIME, DELETEDTIME, VERIFIEDTIME 
 		FROM rs_user
 		
 	</select>
 	
	<select id="readUser" resultMap="User">
       SELECT ID, NAME, EMAIL, TYPE, ISVERIFIED, ISDELETED, REGISTEREDTIME, DELETEDTIME, VERIFIEDTIME 
       FROM rs_user 
       WHERE EMAIL = #email# 
       <isNotEmpty property="password">
       and PASSWORD = #password#
       </isNotEmpty>
	</select>

	<insert id="createUser" parameterClass="com.respace.domain.RS_User">
		 INSERT INTO
		 rs_user (EMAIL, PASSWORD, TYPE)
		 VALUES (#email#, #password#, #type#)
	</insert>
	
	<update id="updateUser" parameterClass="com.respace.domain.RS_User">
		 UPDATE rs_user SET
		 ID = #id#
		 <isNotEmpty property="password">
		 ,PASSWORD = #password#
		 </isNotEmpty>
		 <isNotEmpty property="type">
		 ,TYPE = #type#
		 </isNotEmpty>
		 <isNotEmpty property="isverified">
		 ,ISVERIFIED = #isverified#
		 ,VERIFIEDTIME = DATE_FORMAT(now(), "%Y%m%d%H%i%s")
		 </isNotEmpty>
		 <isNotEmpty property="isdeleted">
		 ,ISDELETED = #isdeleted#
		 ,DELETEDTIME = DATE_FORMAT(now(), "%Y%m%d%H%i%s")
		 </isNotEmpty>
		 WHERE ID = #id# 
	</update>
</sqlMap>


